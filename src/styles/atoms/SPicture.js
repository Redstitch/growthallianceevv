import styled from 'styled-components';

const SPicture = styled.div`
  picture {
    width: 100%;
    display: block;

    img {
      display: block;
      width: 100%;
      height: auto;
    }
  }
`;

export default SPicture;
